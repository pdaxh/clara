apiVersion: v1
kind: ConfigMap
metadata:
  name: clara-config
  labels:
    app: clara
data:
  google-cloud-project: ""
  log-level: "INFO"
  debug: "false"
  # Language setting: 'en' for English, 'sv' for Swedish
  # Default: 'en'
  language: "en"

