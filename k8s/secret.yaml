apiVersion: v1
kind: Secret
metadata:
  name: clara-secrets
  labels:
    app: clara
type: Opaque
stringData:
  # Replace with actual Google ADK API key
  # kubectl create secret generic clara-secrets --from-literal=google-api-key=your-key-here
  # NOTE: For production, use Sealed Secrets (see secret-sealed.yaml)
  google-api-key: ""

