apiVersion: v1
kind: Secret
metadata:
  name: clara-secrets
  labels:
    app: clara
type: Opaque
stringData:
  # Replace with actual OpenAI API key
  # kubectl create secret generic clara-secrets --from-literal=openai-api-key=your-key-here
  openai-api-key: ""

